import { createMuiTheme } from '@material-ui/core/styles'

const theme = createMuiTheme({
  palette: {
    primary: {
      // oranje
      main: 'rgb(241, 108, 33)',
    },
    secondary: {
      // donkerblauw
      main: 'rgb(0, 56, 89)',
    },
  },
})

export default theme
